import dashboard from './dashboard';
import products from './product'
import reports from './reports'

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, products, reports]
};

export default menuItems;
